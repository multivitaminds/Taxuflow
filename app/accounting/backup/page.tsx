import BackupRestoreClient from "./BackupRestoreClient" // Assuming BackupRestoreClient is a component in the same directory

export default function BackupRestorePage() {
  return <BackupRestoreClient />
}
